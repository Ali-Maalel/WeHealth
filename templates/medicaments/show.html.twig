{% extends 'base.html.twig' %}

{% block title %}Medicaments{% endblock %}

{% block body %}
    <h1>Medicaments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medicament.name }}</td>
            </tr>
            <tr>
                <th>Mode_utilisation</th>
                <td>{{ medicament.modeUtilisation }}</td>
            </tr>
            <tr>
                <th>Type_medicaments</th>
                <td>{{ medicament.typeMedicaments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicaments_index') }}">back to list</a>

    <a href="{{ path('app_medicaments_edit', {'id': medicament.id}) }}">edit</a>

    {{ include('medicaments/_delete_form.html.twig') }}
{% endblock %}
