<form method="post" action="{{ path('app_num_media_delete', {'id': num_media.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ num_media.id) }}">
    <button class="btn">Delete</button>
</form>
