<form method="post" action="{{ path('app_professionnel_sante_delete', {'id': professionnel_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professionnel_sante.id) }}">
    <button class="btn">Delete</button>
</form>
